---
kind: Service
apiVersion: v1
metadata:
  name: {{cookiecutter.role_name}}
  namespace: {{ "{{" }} {{cookiecutter.var_prefix}}_namespace {{ "}}" }}
spec:
  selector:
    name: {{cookiecutter.role_name}}
  ports:
  - name: http
    port: 80
    targetPort: http
